<% if @current_user.present? %>

	<h1>ALREADY LOGGED IN</h1>


<% else %>

	<div class="row sign-up-background">
		<div class="small-12 medium-8 medium-offset-4 large-6 large-offset-6 columns sign-up-box">
			<h3>Sign Up Here!</h3>
			<%= render :partial => 'form' %>
		</div>
	</div>

<% end %>