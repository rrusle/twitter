<p>
  <strong>User:</strong>
  <%= @tweet.user.name if @tweet.user %>
</p>

<p>
  <strong>tweet:</strong>
  <%= @tweet.content %>
</p>
	<% if @current_user.try(:id) == @tweet.user_id %>
<p>
	<%= link_to 'Edit', edit_tweet_path(@tweet)  %> |
	<%= link_to 'Back', tweets_path %>| 
	<%= link_to 'Delete', @tweets, :method => :delete, :data => {:confirm => 'Are you sure?'}  %>
</p> 
	<% end %>

	